import { LayoutAdmin } from "@/components/layout";

const AdminProducts = () => (
  <LayoutAdmin title="Produkty | Panel administratora">
    Panel administratora - produkty
  </LayoutAdmin>
);

export default AdminProducts;
