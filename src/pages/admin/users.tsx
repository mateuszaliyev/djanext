import { LayoutAdmin } from "@/components/layout";

const AdminUsers = () => (
  <LayoutAdmin title="Użytkownicy | Panel administratora">
    Panel administratora - użytkownicy
  </LayoutAdmin>
);

export default AdminUsers;
